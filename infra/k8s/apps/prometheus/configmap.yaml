apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'demo-api'
        metrics_path: /metrics
        static_configs:
          - targets: ['demo-api:8080']
      - job_name: 'predictor'
        metrics_path: /metrics
        static_configs:
          - targets: ['predictor:8080']
      - job_name: 'diagnosis-agent'
        metrics_path: /metrics
        static_configs:
          - targets: ['diagnosis-agent:8080']
      - job_name: 'remediator'
        metrics_path: /metrics
        static_configs:
          - targets: ['remediator:8080']
      - job_name: 'api'
        metrics_path: /metrics
        static_configs:
          - targets: ['api:8080']
