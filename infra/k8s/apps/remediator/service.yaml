apiVersion: v1
kind: Service
metadata:
  name: remediator
  labels:
    app.kubernetes.io/name: remediator
spec:
  selector:
    app.kubernetes.io/name: remediator
  ports:
    - name: http
      port: 8080
      targetPort: http
